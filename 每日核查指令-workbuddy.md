# 二游活动日历 · 每日核查指令

> 执行 agent：WorkBuddy（需多模态视觉 + 联网搜索能力）
> 用途：作为每日定时任务的执行内容，每天核查一次
> 项目目录：`d:\xia zai wen jian\Download\YOYUXI`

---

## 任务概述

你是「二游活动日历」的核查助手，每天定时执行一次，独立完成：

1. 服务检查
2. 逐游戏搜索核实新活动
3. 图片型排期识别（多模态识图）
4. 活动卡切割配图（PIL）
5. 更新日历数据（manual.json）
6. 清理过期条目与孤儿图
7. 触发服务刷新
8. 汇报

覆盖游戏：崩坏：星穹铁道（hsr）、绝区零（zzz）、明日方舟：终末地（endfield）、明日方舟（arknights）、鸣潮（wuwa）、异环（ananta）、重返未来：1999（r1999）

---

## 最高优先级禁令

本任务**唯一允许修改的文件是 `data\manual.json` 与 `static\img\` 下的切割产物目录**，除此之外禁止写入/修改任何文件——包括 README.md、README_EN.md、README_JA.md、CHANGELOG.md、所有代码与配置文件。

此前发生过自动化运行违规重写 README.md 导致文档内容回退的事故，绝对禁止再犯。不需要 git 提交或推送。汇报中不必提及此禁令。

---

## 【第 1 步 · 确保本地服务在线】

1. 探测服务：用 curl 或等价工具请求 `http://127.0.0.1:5000/api/events`（超时 3 秒），期望返回 200 与 JSON
2. 若探测失败（服务未运行），用**分离进程**后台启动（**不要用前台 `python app.py`**——前台会阻塞执行，且服务会随本任务结束被回收，导致之后用户打开 App 图片全挂）：
   ```powershell
   Start-Process -WindowStyle Hidden -FilePath "py" -ArgumentList "app.py" -WorkingDirectory "d:\xia zai wen jian\Download\YOYUXI"
   ```
   等待约 10 秒后再次探测确认可访问
3. 服务已在线则跳过，**绝不要重启已在运行的服务**
4. 若启动仍失败，如实汇报启动失败原因，然后继续执行后续核查步骤（核查不依赖服务在线）

---

## 【第 2 步 · 逐游戏联网搜索】

核查 7 款游戏**最近 1~2 天官方新发布**的活动与卡池排期。每个关键词搜一次，按时间排序取最新结果。

### 搜索关键词清单

| 游戏 | game_id | 搜索关键词 |
|------|---------|-----------|
| 崩坏：星穹铁道 | hsr | `星穹铁道 版本活动一览`、`星穹铁道 新版本 前瞻直播 时间`、`星穹铁道 版本 卡池 跃迁 时间`、`星穹铁道 签到 活动 时间` |
| 绝区零 | zzz | `绝区零 新版本 活动 时间`、`绝区零 版本前瞻特别节目 直播`、`绝区零 调频 卡池 时间`、`绝区零 签到 活动` |
| 明日方舟：终末地 | endfield | `明日方舟终末地 活动 排期`、`明日方舟终末地 版本前瞻 直播`、`明日方舟终末地 寻访申领 时间` |
| 明日方舟 | arknights | `明日方舟 版本活动 时间`、`明日方舟 新活动 干员 寻访 时间`、`明日方舟 签到 累计登录 活动`、`明日方舟 SideStory 活动 时间` |
| 鸣潮 | wuwa | `鸣潮 版本更新活动`、`鸣潮 新版本 前瞻 直播 时间`、`鸣潮 角色唤取 卡池 时间`、`鸣潮 签到 活动` |
| 异环 | ananta | `异环 版本活动`、`异环 新版本 前瞻 时间`、`异环 研募 卡池 时间` |
| 重返未来：1999 | r1999 | `重返未来1999 版本活动`、`重返未来1999 新版本 前瞻 时间`、`重返未来1999 角色征集 卡池 时间`、`重返未来1999 签到 活动` |

### 重点查找内容

- 新版本「活动一览 / 更新说明」
- 限时活动
- 角色与专武卡池（祈愿 / 跃迁 / 调频 / 唤取 / 寻访申领）
- 每日签到 / 累计登录类活动的起止时间

### 核实标准

- 起止时间必须有官方来源交叉确认（官网新闻、官方 B站号、游戏内公告转载、官方抖音/小红书号，**至少两处一致**）
- 时间按北京时间精确到 HH:MM
- 无法核实的活动一律不录入，**宁缺毋滥**
- 搜索无果或时间存疑 → 换词重搜 1 次，仍无 → 该游戏标记「无新增 / 存疑」

---

## 【第 3 步 · 官方动态巡查与配图（必做，禁止跳过）】

**配图是硬性要求：新录入条目 image 留空 = 本次任务不合格。数据源 A / B / C 必须逐级检查（每个游戏都要过一遍），D 尽力而为。搜索引擎只负责发现线索，图片必须从下面的具体页面/缓存中提取。**

**★ 缺图归零强制（2026-09-19 新增，最高优先）**：当前 manual.json 还有约 41 条缺图（hsr 6 / wuwa 3 / ananta 3 / endfield 3 / r1999 19 / zzz 5 / arknights 2），本次任务**必须为其中至少 10 条配上图**，汇报里单独列「本次清零清单」。原则：优先从 B站官号历史动态（browser_use 滚动）与米游社公告页找该活动的**专属宣传图**；找不到专属图时，允许用该游戏当前版本 KV 或版本内容一览长图**切割出的对应区段**充当兜底图（每条目裁不同区段，禁止两条目挂同一文件）；两者都没有才允许留空，且汇报必须给出该条目查过的具体 URL 列表。

**各渠道实测情报（2026-09-19，直接照做，不要重复试错）**：
- ✅ **异环 yh.wanmei.com**：requests 抓详情页原始 HTML，`<img src>` 即官方图床直链（白名单内）
- ✅ **终末地 endfield.hypergryph.com/news**：SSR 页内嵌转义 JSON（`\"title\":\"..\",\"cover\":\"https://web.hycdn.cn/..\"`），还原 `\"` 后正则提取；hycdn.cn 防盗链，下载需带 `Referer: https://endfield.hypergryph.com/`；列表翻页需 browser_use 点击
- ✅ **米游社公告**（hsr 深渊/虚构叙事等周期玩法的图在这里）：browser_use 打开 `https://www.miyoushe.com/sr/` 搜索活动名，进公告页取正文图；也可用 browser_use 滚 `https://space.bilibili.com/1340190821/dynamic`
- ❌ **hsr 官网公告 API**（hkrpg-ann-api getAnnContent）：ann_id 参数被忽略，永远返回调研问卷——不要再用
- ❌ **鸣潮 mc.kurogame.com**：纯 SPA 且 mc-api 需签名 401——只能 B站/browser_use；wuwa 缺的 3 条（烟云赠礼/群声共振模拟域/清弦纪流年）图在 8/20-8/22 的 3.6 内容介绍长图动态里，超出 opus_feed 20 条窗口，必须 browser_use 滚动
- ❌ **r1999 re.bluepoch.com**：官网空壳 + api 404——19 条缺图全靠 browser_use 滚 B站官号 1197454103 的 4.0 版本历史动态（9/17~9/24 发布，含活动一览长图）

### 数据源 A：后端 B站缓存（首选，零成本，必查）

`data/cache.json` 里已有后端 B站爬虫自动抓取的 7 个官方号最新动态，**条目自带封面图**（hdslb.com 直链）：

1. 读取 `data/cache.json`，筛出 `src` 为 `bilibili` 的条目（`image` 字段即图链，`link` 字段是 opus 详情页）
2. 与 `manual.json` 比对（**两件事都必做**）：
   - **补录**：cache 里有、manual 里没有的活动 → 录入
   - **补图**：manual 里该游戏的缺图条目，与 cache 中该游戏的带图动态逐条比对。**标题不必一致**——一条「前瞻特别节目 | 情报回顾」类动态往往是一张超长图，里面塞着整批活动的详情卡。判定步骤：**用 `browser_use` 子代理打开该动态 opus 详情页**（cache 的 `link` 字段，opus 页同样是 SPA，WebFetch 拿不到）提取**全部配图** → 多模态看图内容 → 与缺图条目对应（角色立绘/棋盘宣传图 → 对应卡池条目；活动界面图 → 同名活动；签到界面 → 签到条目）→ **长图必须按第 4 步切成独立卡后分别挂载**，不得整图重复挂给多个条目
   - 缺图条目在所有数据源穷尽后仍无图，才允许留空并汇报原因
3. 需要多模态看图的（终末地/1999 的排期长图），把图链经 `http://127.0.0.1:5000/api/img?url=<URL编码>` 代理后读图

### 数据源 B：B站官方号动态页（必查）

各游戏官方号动态页（UID 已核实，直接用）：

| 游戏 | 官方号动态页 |
|------|-------------|
| 崩坏：星穹铁道 | https://space.bilibili.com/1340190821/dynamic |
| 绝区零 | https://space.bilibili.com/1636034895/dynamic |
| 明日方舟：终末地 | https://space.bilibili.com/1265652806/dynamic |
| 明日方舟 | https://space.bilibili.com/161775300/dynamic |
| 鸣潮 | https://space.bilibili.com/1955897084/dynamic |
| 异环 | https://space.bilibili.com/3546636978489848/dynamic |
| 重返未来：1999 | https://space.bilibili.com/1197454103/dynamic |

1. **用 Task 工具派 `browser_use` 子代理打开动态页滚动查找**（不要用 WebFetch——B站动态页是 SPA，WebFetch 只能拿到空壳页脚）：派发指令写「打开 `https://space.bilibili.com/<UID>/dynamic`，向下滚动加载（每次约 10 条），查找含「<活动名>」的动态，截图转写其完整正文、发布时间与全部配图直链；未找到则滚到页尾并如实回报已滚动几屏」。**注意：后端 API 只能拿到最新 20 条，更早的动态只有浏览器滚动才能看到**
2. cache 条目 `link` 指向的 opus 详情页（`https://www.bilibili.com/opus/<id>`）同样派 browser_use 子代理打开，提取完整正文与**全部配图**（活动详情图/长图往往不止封面一张）；图片点开放大后从浏览器地址栏读直链（形如 `https://i0.hdslb.com/bfs/new_dyn/xxx.jpg`）
3. 提取出的活动一览图/长图，经 `/api/img` 代理交给多模态按下方「长图排期识别规则」转写

### 数据源 C：游戏官网（必查，信息最全最权威）

官网新闻页是活动的第一发布渠道，图文最全、规则最细。各游戏官网新闻入口（域名已从后端爬虫源码核实，直接用）：

| 游戏 | 官网新闻入口 |
|------|-------------|
| 崩坏：星穹铁道 | https://sr.mihoyo.com/news/ （抓不到时 WebSearch `site:sr.mihoyo.com <活动名>`） |
| 绝区零 | https://zzz.mihoyo.com/news/ （或 WebSearch `site:zzz.mihoyo.com <活动名>`） |
| 明日方舟：终末地 | https://endfield.hypergryph.com/news |
| 明日方舟 | https://ak.hypergryph.com/news |
| 鸣潮 | https://mc.kurogame.com （新闻详情页 /main/news/detail/<id>） |
| 异环 | https://yh.wanmei.com/news （栏目页形如 /news/<cat>/index.html） |
| 重返未来：1999 | https://re.bluepoch.com （详情页形如 /home/detail.html#newsId?<id>）；**若该站 404/串站不可达，改用 B站官号动态页 1197454103 与官方微博，并在汇报中注明官网不可达** |

要求：**每个游戏至少用 Task 工具派 `browser_use` 子代理打开一次官网新闻列表页**，扫最近 1~2 天的新条目；发现新活动必须进详情页提取图文（活动一览图、规则说明图都在这里）。米哈游/库洛等站点为 JS 动态渲染，**WebFetch 会返回空内容，不得用它代替浏览器**。

**★ 官网原始 HTML 提图技巧（2026-09-19 实战验证，优先级高于浏览器）**：很多"看似 SPA"的官网详情页，用 `requests`（需 `trust_env=False` 绕系统代理）抓**原始 HTML** 后，能直接拿到图片直链：
- **异环 yh.wanmei.com**：详情页 HTML 里 `<img src>` 就是官方图床直链（`yh.wanmei.com/resources/jpg/日期/xxx.jpg`，在服务端代理白名单内）。WebFetch 会把图转成它自己的缓存域名（aka.doubaocdn.com）导致拿不到原链，**必须 requests 抓原始 HTML 再正则提 `<img src>`**
- **终末地 endfield.hypergryph.com**：列表页 HTML 是 SSR，内嵌转义 JSON（形如 `{\"cid\":\"..\",\"title\":\"..\",\"cover\":\"https://web.hycdn.cn/...\"}`），把 `\"` 还原成 `"` 后正则提取全部 标题→封面 映射；hycdn.cn 图床有防盗链，需带 `Referer: https://endfield.hypergryph.com/` 下载落盘，或经 `/api/img` 代理（hycdn.cn 已加入白名单）
- **鸣潮 mc.kurogame.com**：详情页是纯 SPA 空壳且 API 需签名（401），只能走 B站动态或 browser_use
- 抓到图后按「image 字段填法」回填

### 数据源 D：抖音 / 小红书（必试，按能力逐级降级）

按以下顺序尝试，走到哪级算哪级，如实汇报：

1. **WebSearch 文字核实（必做）**：`<游戏名> <活动名> 官方 抖音` / `小红书`——标题、发布时间、活动名可作交叉确认来源
2. **WebFetch 详情页（尝试）**：抖音/小红书有登录墙和 JS 渲染，抓取失败属正常，记录「平台页不可达」即可，不得卡住流程
3. **Task 工具派 `browser_use` 子代理（必做）**：用浏览器打开官方账号主页翻最近动态，这是唯一能稳定拿到这两个平台配图的方式。抖音/小红书有登录墙，若确实打不开，如实记录「需登录，不可达」。

### 长图排期识别规则（多模态）

1. 用多模态视觉能力直接读图，逐段浏览覆盖全图，转写图中所有活动的**名称、类型、起止日期**及其在整图中的上下边界（供第 4 步切割用）。**若执行模型无视觉能力：用 Task 工具派 `browser_use` 子代理执行同样的看图转写流程**（子代理打开排期图直链，逐屏截图并转写）
2. 对转写结果做合理性校验：活动日期应落在当前版本周期内、与已录入条目不矛盾；冲突的放大该横条重新核实（重新识图或再派 browser_use 子代理放大该横条）
3. 时分读不出时，按该游戏版本更新的惯例时刻补齐（终末地为 **12:00 更新、次日 11:59 结束**），并在汇报中注明「时分按惯例补齐」
4. 看不清的条目标注「不确定」，**禁止编造**；绝不让主会话臆测图片内容——图片信息只能来自真实识图或子代理截图转写
5. 仅当多模态与 browser_use 子代理**均不可用**时，才退回纯文字搜索，并在汇报中列出未能识别的图片 URL

### 防盗链规则

hdslb.com 等带 Referer 防盗链的图床，一律经 `http://127.0.0.1:5000/api/img?url=<URL编码>` 代理给多模态看图，禁止直接 fetch 图片 URL。

---

## 【第 4 步 · 活动卡切割配图（多模态定位 + PIL，全自动）】

官方前瞻/排期图多为「一张超长纵图塞几十个活动」（典型：终末地单图高达 39760px），整图缩略几乎看不清，需要按活动切成独立卡片：

1. **用多模态视觉定位**：把长图按固定高度切成若干段预览图 → 逐段看图确认每个独立活动/角色的标题、类型、开启时间及其上下边界，输出各活动在整图中的**百分比/像素区间**（无视觉能力时派 `browser_use` 子代理逐段截图转写）
2. 用 **Python PIL** 按边界把原图（保持原始分辨率）裁剪成每活动一张独立卡，文件名用拼音，保存到 `static/img/<game_id>/` 目录
3. 把 `manual.json` 里对应条目的 image 字段改成该相对路径（如 `/static/img/zzz/diaopin_diyiqi_kelalei.jpg`）；无对应卡的条目 image 留空字符串
4. **★关键规则（务必看清，此前被误读）**：合并长图**必须切分后使用**——一张图内含多个独立条目时（如一期调频图含多个角色池、活动一览图含多个活动），按每个条目的边界**分别切出独立卡**，每个条目挂它自己那张卡。这里禁止的是「**不切分就直接把同一张图挂给多个条目**」，**绝不是禁止使用合并图**。凡长图中能看清的活动卡，一律切割后配图，**不得因为"它在合并图里"就留空**。
5. **每个缺图条目，先派 `browser_use` 子代理找独立图**：到该游戏 B站官号动态页滚动搜索该活动名，确认是否存在独立宣传图（联动、卡池、签到类活动通常都有单独动态）。有独立图直接用独立图；没有独立图、且长图里也没有对应卡片，才允许留空并逐条说明原因。**留空是最后手段，不是省事的默认选项**。
6. 同一活动跨段展示的（如签到卡 + 奖励卡）可合并或分块按需处理；切割后确认静态路径经 `http://127.0.0.1:5000/static/img/<game_id>/<文件名>` 可访问
7. **切割后自检（多模态）**：全部切割完成后，用多模态视觉能力**逐张打开切割产物卡**，与 manual.json 里该条目的标题/类型逐一比对，确认：① 每张卡内容与对应条目一致（无错切、无混入相邻活动）② 每个条目都挂到了自己的卡、没有漏挂或重复挂 ③ 切割边界完整（无截断标题、无裁掉时间行）。发现切割错误或对应关系错误的，重新定位边界并重新切割修正；自检结果在汇报中说明（切割 N 张卡、自检通过 / 修正了几张）
8. 不依赖任何第三方 API，全靠模型视觉能力 + PIL；无视觉能力时派 `browser_use` 子代理截图核对，二者都没有才跳过本步、整图缩略即可

---

## 【第 5 步 · 更新 `data\manual.json`】

1. 先**完整读取**该文件现有条目
2. **去重**：新活动与现有条目按（game_id + 标题）对比，已存在的不重复添加；标题仅标点/括号差异的视为同一条；图片识别出的条目若与现有条目为同一活动，**更新**现有条目的时间与 image 字段而不是新增
3. 新增条目追加到 JSON 数组末尾，字段格式：

```json
{"game_id": "hsr", "title": "「活动名」", "category": "活动", "start": "2026-09-13T10:00", "end": "2026-09-27T03:59", "link": "官方链接", "image": ""}
```

字段规则：

- `category` 取值：活动 / 签到 / 角色与专武 / 公告 / 资讯
  - 角色、专武卡池一律用「角色与专武」
  - 每日签到/累计登录/打卡类活动一律用「签到」
- `image` 优先填切割卡相对路径（`/static/img/<game_id>/xxx.jpg`）；无切割卡时填官方配图或排期原图直链 URL；**新录入条目禁止直接留空**——必须先走第 3 步的「配图获取链」，穷尽后仍无图才允许填 `""`，并在汇报中列出缺图条目及原因
- 常驻或无结束时间的活动 `end` 填空字符串 `""`
- 只录入结束时间在当前时间之后的活动；即将上线的新版本可只填 start、end 留空

4. **清理过期**：删除所有 end 为有效日期且早于当前时间的条目；end 为空字符串的常驻条目保留不动
5. **清理孤儿图**：扫描 `static/img/` 下所有图片文件，删除其中未被 `manual.json` 任何条目的 image 字段引用（且未被 `cache.json` 引用）的图片文件；被引用或不确定是否引用的保留，**绝不删除静态目录之外的任何文件**
6. 以 UTF-8 保存，保证 JSON 数组格式合法，不要改动其他既有条目

---

## 【第 6 步 · 触发服务刷新】

- 服务每 30 分钟自动重载 `manual.json`，无需重启
- 若第 1 步确认服务在线，触发立即刷新：

```bash
curl -X POST http://127.0.0.1:5000/api/refresh -H "X-Requested-With: ycal"
```

- 服务不在线则跳过，注明下次启动后生效

---

## 【第 7 步 · 汇报】

最后用中文简要汇报：

1. 服务在线状态
2. 每个游戏新录入的活动（数量 + 标题列表）
3. 清理了多少条过期条目、删除了多少张孤儿图
4. 无新增的游戏直接说明「无新增」
5. 图片排期识别情况：识别了哪几张排期图、转写录入多少条、修正了哪些现有条目的时间
6. 活动卡切割情况（若本次执行了切割）：切割了哪些活动卡、存到哪个目录、更新了多少条 image 字段
7. 配图覆盖情况：本次录入/更新的条目中有图 X 条、缺图 Y 条；有图的注明来源层级（A 后端缓存 / B B站opus / C 官网），缺图的逐条列出并说明哪一级没走通（数据源 D 防盗链失败属正常，如实记录即可）

某游戏搜索后拿不准时间的，明确说明存疑而不要录入。

---

## 附：若执行环境无法访问本地文件

如果 WorkBuddy 是独立环境、读不到你电脑上的 `data/manual.json`：

- 第 5 步改为：只输出**完整 JSON 数组原文**（含新增 + 更新 + 删减后的全量内容），由你手动粘贴覆盖文件
- 第 4 步活动卡切割、第 6 步服务刷新同样无法执行 → 跳过并在汇报注明

---

*数据版权声明：本项目展示的全部活动排期与公告资讯，其内容版权完全归各游戏官方（米哈游、鹰角网络、库洛游戏、完美世界等）所有。本项目仅为个人学习交流用途的信息聚合展示，不用于任何商业目的；各活动详情请以游戏官方发布为准。*

# 二游活动日历 · 每日核查指令

> 执行 agent：WorkBuddy（需多模态视觉 + 联网搜索能力）
> 用途：作为每日定时任务的执行内容，每天核查一次
> 项目目录：`d:\xia zai wen jian\Download\YOYUXI`

---

## 任务概述

你是「二游活动日历」的核查助手，每天定时执行一次，独立完成：

1. 服务检查
2. 逐游戏搜索核实新活动
3. 图片型排期识别（多模态识图）
4. 活动卡切割配图（PIL）
5. 更新日历数据（manual.json）
6. 清理过期条目与孤儿图
7. 触发服务刷新
8. 汇报

覆盖游戏：崩坏：星穹铁道（hsr）、绝区零（zzz）、明日方舟：终末地（endfield）、明日方舟（arknights）、鸣潮（wuwa）、异环（ananta）、重返未来：1999（r1999）

---

## 最高优先级禁令

本任务**唯一允许修改的文件是 `data\manual.json` 与 `static\img\` 下的切割产物目录**，除此之外禁止写入/修改任何文件——包括 README.md、README_EN.md、README_JA.md、CHANGELOG.md、所有代码与配置文件。

此前发生过自动化运行违规重写 README.md 导致文档内容回退的事故，绝对禁止再犯。不需要 git 提交或推送。汇报中不必提及此禁令。

---

## 【第 1 步 · 确保本地服务在线】

1. 探测服务：用 curl 或等价工具请求 `http://127.0.0.1:5000/api/events`（超时 3 秒），期望返回 200 与 JSON
2. 若探测失败（服务未运行），在项目目录后台启动服务：
   ```bash
   cd /d "d:\xia zai wen jian\Download\YOYUXI"
   python app.py
   ```
   等待约 10 秒后再次探测确认可访问
3. 服务已在线则跳过，**绝不要重启已在运行的服务**
4. 若启动仍失败，如实汇报启动失败原因，然后继续执行后续核查步骤（核查不依赖服务在线）

---

## 【第 2 步 · 逐游戏联网搜索】

核查 7 款游戏**最近 1~2 天官方新发布**的活动与卡池排期。每个关键词搜一次，按时间排序取最新结果。

### 搜索关键词清单

| 游戏 | game_id | 搜索关键词 |
|------|---------|-----------|
| 崩坏：星穹铁道 | hsr | `星穹铁道 版本活动一览`、`星穹铁道 新版本 前瞻直播 时间`、`星穹铁道 版本 卡池 跃迁 时间`、`星穹铁道 签到 活动 时间` |
| 绝区零 | zzz | `绝区零 新版本 活动 时间`、`绝区零 版本前瞻特别节目 直播`、`绝区零 调频 卡池 时间`、`绝区零 签到 活动` |
| 明日方舟：终末地 | endfield | `明日方舟终末地 活动 排期`、`明日方舟终末地 版本前瞻 直播`、`明日方舟终末地 寻访申领 时间` |
| 明日方舟 | arknights | `明日方舟 版本活动 时间`、`明日方舟 新活动 干员 寻访 时间`、`明日方舟 签到 累计登录 活动`、`明日方舟 SideStory 活动 时间` |
| 鸣潮 | wuwa | `鸣潮 版本更新活动`、`鸣潮 新版本 前瞻 直播 时间`、`鸣潮 角色唤取 卡池 时间`、`鸣潮 签到 活动` |
| 异环 | ananta | `异环 版本活动`、`异环 新版本 前瞻 时间`、`异环 研募 卡池 时间` |
| 重返未来：1999 | r1999 | `重返未来1999 版本活动`、`重返未来1999 新版本 前瞻 时间`、`重返未来1999 角色征集 卡池 时间`、`重返未来1999 签到 活动` |

### 重点查找内容

- 新版本「活动一览 / 更新说明」
- 限时活动
- 角色与专武卡池（祈愿 / 跃迁 / 调频 / 唤取 / 寻访申领）
- 每日签到 / 累计登录类活动的起止时间

### 核实标准

- 起止时间必须有官方来源交叉确认（官网新闻、官方 B站号、游戏内公告转载，**至少两处一致**）
- 时间按北京时间精确到 HH:MM
- 无法核实的活动一律不录入，**宁缺毋滥**
- 搜索无果或时间存疑 → 换词重搜 1 次，仍无 → 该游戏标记「无新增 / 存疑」

---

## 【第 3 步 · 图片型排期识别（多模态识图）】

部分游戏（典型：**明日方舟：终末地、重返未来：1999**）把活动排期做成图片发布，WebSearch/WebFetch 只返回文字、看不到图片。

1. 从抓取缓存 `data/cache.json` 或搜索结果中找到「版本日历 / 活动一览」图片的直链 URL（hdslb.com、hycdn.cn 等图床，可从官方 B站动态 / 详情页拿到高清原图）
2. 用多模态视觉能力直接读图，逐段滚动浏览覆盖全图，转写图中所有活动的**名称、类型、起止日期**及其在整图中的上下边界（供切割用）
3. 对转写结果做合理性校验：活动日期应落在当前版本周期内、与已录入条目不矛盾；冲突的放大该横条重新核实
4. 时分读不出时，按该游戏版本更新的惯例时刻补齐（终末地为 **12:00 更新、次日 11:59 结束**），并在汇报中注明「时分按惯例补齐」
5. 看不清的条目标注「不确定」，**禁止编造**
6. 无法读图时退回纯文字搜索，在汇报中列出未能识别的图片 URL

---

## 【第 4 步 · 活动卡切割配图（多模态定位 + PIL，全自动）】

官方前瞻/排期图多为「一张超长纵图塞几十个活动」（典型：终末地单图高达 39760px），整图缩略几乎看不清，需要按活动切成独立卡片：

1. **用多模态视觉定位**：把长图按固定高度切成若干段预览图 → 逐段看图确认每个独立活动/角色的标题、类型、开启时间及其上下边界，输出各活动在整图中的**百分比/像素区间**
2. 用 **Python PIL** 按边界把原图（保持原始分辨率）裁剪成每活动一张独立卡，文件名用拼音，保存到 `static/img/<game_id>/` 目录
3. 把 `manual.json` 里对应条目的 image 字段改成该相对路径（如 `/static/img/zzz/diaopin_diyiqi_kelalei.jpg`）；无对应卡的条目 image 留空字符串
4. **★关键规则**：一张图内若包含多个独立条目（如一期调频图里含多个角色池、活动一览图里含多个活动），必须按每个角色/活动的边界**分别切出独立卡**，每个条目挂它自己那张卡；严禁用同一张合并图同时挂多个条目
5. 同一活动跨段展示的（如签到卡 + 奖励卡）可合并或分块按需处理；切割后确认静态路径经 `http://127.0.0.1:5000/static/img/<game_id>/<文件名>` 可访问
6. **切割后自检（多模态）**：全部切割完成后，用多模态视觉能力**逐张打开切割产物卡**，与 manual.json 里该条目的标题/类型逐一比对，确认：① 每张卡内容与对应条目一致（无错切、无混入相邻活动）② 每个条目都挂到了自己的卡、没有漏挂或重复挂 ③ 切割边界完整（无截断标题、无裁掉时间行）。发现切割错误或对应关系错误的，重新定位边界并重新切割修正；自检结果在汇报中说明（切割 N 张卡、自检通过 / 修正了几张）
7. 不依赖任何第三方 API，全靠模型视觉能力 + PIL；无视觉能力或无 PIL 环境则跳过本步，整图缩略即可

---

## 【第 5 步 · 更新 `data\manual.json`】

1. 先**完整读取**该文件现有条目
2. **去重**：新活动与现有条目按（game_id + 标题）对比，已存在的不重复添加；标题仅标点/括号差异的视为同一条；图片识别出的条目若与现有条目为同一活动，**更新**现有条目的时间与 image 字段而不是新增
3. 新增条目追加到 JSON 数组末尾，字段格式：

```json
{"game_id": "hsr", "title": "「活动名」", "category": "活动", "start": "2026-09-13T10:00", "end": "2026-09-27T03:59", "link": "官方链接", "image": ""}
```

字段规则：

- `category` 取值：活动 / 签到 / 角色与专武 / 公告 / 资讯
  - 角色、专武卡池一律用「角色与专武」
  - 每日签到/累计登录/打卡类活动一律用「签到」
- `image` 优先填切割卡相对路径（`/static/img/<game_id>/xxx.jpg`）；无切割卡时填官方配图或排期原图直链 URL；都没有则空字符串 `""`
- 常驻或无结束时间的活动 `end` 填空字符串 `""`
- 只录入结束时间在当前时间之后的活动；即将上线的新版本可只填 start、end 留空

4. **清理过期**：删除所有 end 为有效日期且早于当前时间的条目；end 为空字符串的常驻条目保留不动
5. **清理孤儿图**：扫描 `static/img/` 下所有图片文件，删除其中未被 `manual.json` 任何条目的 image 字段引用（且未被 `cache.json` 引用）的图片文件；被引用或不确定是否引用的保留，**绝不删除静态目录之外的任何文件**
6. 以 UTF-8 保存，保证 JSON 数组格式合法，不要改动其他既有条目

---

## 【第 6 步 · 触发服务刷新】

- 服务每 30 分钟自动重载 `manual.json`，无需重启
- 若第 1 步确认服务在线，触发立即刷新：

```bash
curl -X POST http://127.0.0.1:5000/api/refresh -H "X-Requested-With: ycal"
```

- 服务不在线则跳过，注明下次启动后生效

---

## 【第 7 步 · 汇报】

最后用中文简要汇报：

1. 服务在线状态
2. 每个游戏新录入的活动（数量 + 标题列表）
3. 清理了多少条过期条目、删除了多少张孤儿图
4. 无新增的游戏直接说明「无新增」
5. 图片排期识别情况：识别了哪几张排期图、转写录入多少条、修正了哪些现有条目的时间
6. 活动卡切割情况（若本次执行了切割）：切割了哪些活动卡、存到哪个目录、更新了多少条 image 字段

某游戏搜索后拿不准时间的，明确说明存疑而不要录入。

---

## 附：若执行环境无法访问本地文件

如果 WorkBuddy 是独立环境、读不到你电脑上的 `data/manual.json`：

- 第 5 步改为：只输出**完整 JSON 数组原文**（含新增 + 更新 + 删减后的全量内容），由你手动粘贴覆盖文件
- 第 4 步活动卡切割、第 6 步服务刷新同样无法执行 → 跳过并在汇报注明

---

*数据版权声明：本项目展示的全部活动排期与公告资讯，其内容版权完全归各游戏官方（米哈游、鹰角网络、库洛游戏、完美世界等）所有。本项目仅为个人学习交流用途的信息聚合展示，不用于任何商业目的；各活动详情请以游戏官方发布为准。*
